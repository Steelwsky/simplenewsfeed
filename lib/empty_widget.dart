import 'package:flutter/material.dart';

class StarterEmptyList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [Text('Swipe down to update the feed')],
    );
  }
}
